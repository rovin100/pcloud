<!DOCTYPE html>
<html>
<head>
  <title>Pcloud</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
<div id="user_nav">
  <% if user_signed_in? %>
      Signed in as <%= current_user.email %>. Not you?
      <%= link_to "Sign out", destroy_user_session_path,:method => :delete %>
  <% else %>
      <%= link_to "Sign up", new_user_registration_path,:class => 'fancybox' %> or
      <%= link_to "Sign in", new_user_session_path,:class => 'fancybox' %>
  <% end %>
  <%= javascript_tag do %>
      $(document).ready(function(){
      $(".fancybox").fancybox();
      })
  <% end %>
</div>
<%= yield %>

</body>
</html>
